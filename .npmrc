# .npmrc — pnpm configuration for performance

# Use faster resolution
resolution-mode=highest

# Reduce network requests
prefer-frozen-lockfile=true

# Parallel installs
child-concurrency=4

# Reduce optional deps
optional=false
